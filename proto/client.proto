syntax = "proto3";

package lunaris.client;

message CreateTask {
  bytes wasm_module = 1;
  string args = 2;
  string entry = 3;   // WASM模块入口点
  int32 priority = 4; // 优先级
}

message UnsubscribeTask { repeated string task_id = 1; }