syntax = "proto3";

package lunaris.client;

import "proto/common.proto";

message CreateTask {
  string code = 1;
  string args = 2;
  common.LuaVersion lua_version = 3; // Lua解释器版本
  int32 priority = 4;                // 优先级
}

message UnsubscribeTask { string task_id = 1; }